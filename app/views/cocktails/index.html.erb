
<%= render 'shared/banner' %>


<h3> The best rated cocktails : </h3>

<ul>

  <% @cocktails.each do |cocktail| %>
    <li>
      <%=link_to cocktail.name, cocktail_path(cocktail) %>
    </li>
  <% end %>
</ul>
<%= render 'shared/card' %>

<%= render 'shared/footer' %>
